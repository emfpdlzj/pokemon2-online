# Shared values loaded by both client and server tooling.
#
# Copy this file to .env for local development. Values in client/.env and
# server/.env override these shared defaults for their own runtime.

POSTGRES_DB=pokemon2
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

POKEMON2_API_BASE=http://localhost:5140
