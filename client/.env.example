# Client-only overrides. Copy to client/.env when needed.
#
# The generated client/env.js is served to the browser, so do not put secrets
# here unless you accept that they are visible to users.

POKEMON2_API_BASE=http://localhost:5140
POKEMON2_LLM_API_KEY=
POKEMON2_LLM_API_URL=https://generativelanguage.googleapis.com/v1beta/models
POKEMON2_LLM_MODEL=gemini-2.0-flash
