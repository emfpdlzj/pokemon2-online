# Backlog

즉시 착수하지 않지만 추적이 필요한 작업, 개선점, 리스크, 아이디어를 기록한다.

## 기록 규칙

- 새 항목은 아래 템플릿으로 추가한다.
- 우선순위는 `P1`, `P2`, `P3` 중 하나로 적는다.
- 상태는 `open`, `doing`, `done`, `dropped` 중 하나로 적는다.
- 실제 착수 대상이 되면 `docs/development-todo.md`에도 반영한다.

## 템플릿

```text
### [P2][open] 항목 제목
- 날짜: YYYY-MM-DD
- 근거:
- 다음 액션:
```

## Entries

### [P1][done] 세이브 데이터 사용자 분리 설계
- 날짜: 2026-06-15
- 근거: 현재 세이브 구조는 사용자 개념 없이 슬롯과 모드 기준으로만 동작해 소유권 분리가 불가능하다.
- 다음 액션: `2026-06-15` 서버 서명 기반 익명 사용자 토큰, `user + mode + slotNumber` 고유 저장, 세이브 소유권 검증, 멀티 재접속 사용자 식별 연동을 반영했다.

### [P2][open] 레거시 공용 세이브 사용자 이전 정책
- 날짜: 2026-06-15
- 근거: 기존 `mode + slotNumber` 기반 저장 데이터는 현재 부팅 시 `legacy-default` 사용자로 백필되어, 실사용 데이터가 이미 있는 환경에서는 사용자별 이전 전략이 별도로 필요하다.
- 다음 액션: 운영 전환 시 기존 세이브를 어떤 사용자에게 귀속시킬지 정책을 정하고, 필요하면 별도 이전 스크립트를 준비한다.

### [P1][done] LLM 서버 운영 보강
- 날짜: 2026-06-15
- 근거: 브라우저 직접 호출 문제는 해소됐지만, 운영 기준에서는 모델 분리, 호출 제한, 비용 추적, fallback 일관성이 추가로 필요했다.
- 다음 액션: `2026-06-15` `/api/llm/reply`, `/api/llm/choices` 기준으로 모델별 설정 분리, 분당 호출 제한, 토큰/예상 비용 누적 집계, Datadog 지표 확장, 캐릭터별 고정 fallback 정책, 운영 문서(`docs/llm-operations.md`)를 반영했다.

### [P2][open] 멀티플레이 재접속 UX
- 날짜: 2026-06-15
- 근거: 연결 종료 시 클라이언트가 내부 상태만 비우고 복구 흐름이 없다.
- 다음 액션: 끊김 감지, 재입장 버튼, 마지막 방 재연결 흐름을 정의한다.

### [P2][open] 소켓 이벤트 UI 정리
- 날짜: 2026-06-15
- 근거: 서버는 `player_joined`, `player_left`, `chat` 이벤트를 보내지만 클라이언트 반영이 부족하다.
- 다음 액션: 최소한의 방 상태 패널과 시스템 메시지 UI를 설계한다.

### [P2][open] 멀티 모드 세이브 슬롯 UI 노출
- 날짜: 2026-06-15
- 근거: 서버 저장 모델은 `single`, `multi` 모드를 지원하지만 현재 세이브 메뉴 조회는 `mode=single`로 고정돼 있다.
- 다음 액션: 멀티 저장/불러오기 메뉴와 슬롯 표시 정책을 정리한다.

### [P2][open] 서버 저장 실패 가시화
- 날짜: 2026-06-15
- 근거: 현재 서버 저장 실패가 사용자에게 드러나지 않아 실제 저장 상태를 알기 어렵다.
- 다음 액션: 저장 상태 배지, 마지막 성공 시각, 재시도 버튼 필요 여부를 검토한다.

### [P2][open] 운영용 admin metrics 보호
- 날짜: 2026-06-15
- 근거: 운영 지표 엔드포인트가 인증 없이 열려 있다.
- 다음 액션: 관리자 토큰 또는 내부망 전용 접근 정책을 정한다.

### [P3][open] DB migration 체계 전환
- 날짜: 2026-06-15
- 근거: 현재 초기화는 `EnsureCreated` 중심이라 스키마 변경 이력 관리에 약하다.
- 다음 액션: EF Core migration 도입 범위와 초기 전환 절차를 정리한다.

### [P3][done] CI 파이프라인 추가
- 날짜: 2026-06-15
- 근거: 테스트는 존재하지만 저장소에 자동 검증 파이프라인이 보이지 않는다.
- 다음 액션: 기본 PR CI는 추가 완료. 필요 시 배포 전용 검증, 커버리지 수집, 브랜치 보호 규칙 연계를 후속으로 확장한다.

### [P2][open] 포트폴리오 소개문 정합성 점검
- 날짜: 2026-06-15
- 근거: 외부 소개문 초안에 현재 저장소에 없는 `Kotlin`, `FastAPI`, `UnityAR`, `STT`가 포함되어 있고, 채팅 동기화는 클라이언트 UX 기준으로는 미완이다.
- 다음 액션: 제출용 소개문에서 실제 저장소 구현과 일치하도록 기술 스택과 기능 문구를 정리한다.
